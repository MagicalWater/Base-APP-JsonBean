name: mx_json
description: A new Flutter plugin.
version: 1.0.0
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  args: "^1.5.1"
  xson_builder: "^1.0.3"

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: "^1.6.8"

executables:
  mxjson: mx_json

flutter:
  plugin:
    androidPackage: com.mg.mx_json
    pluginClass: MxJsonPlugin